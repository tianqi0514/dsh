/**
 * Official client-module discovery follows live Host Loader rows. This root
 * face supplies that lifecycle anchor only; all Plate behavior is in ./client.
 * It registers no Agent, tools, connection, or second business data store.
 */
export const name = 'workdsh-plate';
export function apply(): void {}
